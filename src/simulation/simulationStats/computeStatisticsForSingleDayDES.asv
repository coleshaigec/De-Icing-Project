function singleDayStats = computeStatisticsForSingleDayDES(simContext)
    % COMPUTESTATISTICSFORSINGLEDAYDES Computes results of single-day DES via Monte Carlo estimation.
    
    
    % Stats to compute: a checklist
    % - Number of external arrivals
    % - Total de-icing jobs
    % - Expected de-icing jobs PER EXTERNAL AIRCRAFT
    % - Number of HOT violations
    % - HOT violation rate (number of hot violations per external aircraft)
    % - Expected ground sojourn time per external aircraft
    % - Total de-icing work time (airport level)
    % - Total de-icing queueing delay (airport level)
    % - Expected de-icing service time per external aircraft
    % - Expected de-icing queueing delay (airport level)
    % - Total taxi/takeoff time (airport level)
    % - Total taxi/takeoff queueing delay (airport level)
    % - Expected taxi/takeoff time for external aircraft
    % - Expected taxi/takeoff queueing delay for external aircraft


    % -- Populate output struct --
    singleDayStats = struct();
end
