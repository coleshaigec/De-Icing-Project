function serviceTimeMultiplier = getServiceTimeMultiplierFromPolicyIndex(policyIndex)
    % GETSERVICETIMEMULTIPLIERFROMPOLICYINDEX Returns service time multiplier from specified policy index.x

    % This is a hack that can save time, but it has lots of architectural
    % implications, so be careful
    serviceTimeMultiplier = 1;
end